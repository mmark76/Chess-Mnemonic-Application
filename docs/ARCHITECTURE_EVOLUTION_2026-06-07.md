# Architecture Evolution Update — 2026-06-07

## Purpose

This document updates the architecture direction for the Chess Mnemonic Application after the latest review of `main`, `android-capacitor-setup`, and `experimental/cms-refactor`.

It does not replace `ARCHITECTURE.md`. It extends it safely, without removing any existing baseline information.

The controlling principle remains:

> The code must serve the Chess Mnemonic System.